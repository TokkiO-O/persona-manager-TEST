# Changelog

## v1.10.1

相对 **v1.9.25** 的主要更新：

### 修复
- **列表卡片**：名字/头像/描述正常显示；勾选改为左侧独立列（不再绝对定位盖住名字）
- **头像**：`personaImageUrl(id)` 缩略图
- **全屏编辑**：设置可切换弹窗/全屏；全屏使用 `100dvh`，不再被 visualViewport 误限高
- **设置「编辑框样式」**：分段按钮可点、选中高亮，并有 toast 反馈

### 功能（相对 1.9.25 新增 / 加强）
- 共同片段（字段对齐、中英同义、性格特质）
- 多选对比 / 换基准
- 删除人设（确认、备份、多选）
- 备注/短 ID 区分同名
- 检查更新、版本号日志

### 说明
- 安装后请 **Ctrl+F5**，确认左上角 **v1.10.1**
- 卡片布局：`[勾选] [头像] [名字+描述] [编辑/删除]`
