# Changelog

## v1.10.1

相对 **v1.9.25** 的主要更新：

### 修复（本版）
- **列表卡片**：直接采用 **v1.9.25** 的卡片 DOM + 整份列表相关样式（不再在冲突 CSS 上打补丁）
- **头像**：`personaImageUrl(id)`（与 1.9.25 相同）
- **全屏编辑**：在 1.9.25 样式上追加 `is-fullscreen` → `100dvh`；全屏时不做 visualViewport 限高

### 功能（相对 1.9.25 仍保留）
- 共同片段按字段对齐、中英同义、性格逗号特质
- 多选对比 / 换基准 / 删除（确认·备份·多选）
- 备注/短 ID 区分；设置里编辑框弹窗/全屏；检查更新

### 说明
安装后请 **Ctrl+F5**，确认左上角 **v1.10.1**。列表应与 1.9.25 一样能看见头像、名字、描述。
