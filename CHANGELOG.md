# Changelog

## v1.9.25

- **列表 UI**
  - 横排信息卡（勾选 + 头像 + 文案 + 右侧图标操作）
  - 默认「舒适」密度（更高卡片、更大头像），可切换「紧凑」
  - 同名组超过 4 个默认收起，支持展开全部
  - 空状态按全部 / 同名 / 重复 / 相似分场景文案
  - 统计胶囊化；标签页激活态指示条
- **页头与底栏**
  - 刷新与关闭并到右上角
  - 选中底栏提高层级并留底，避免挡住「开始对比」
- **编辑**
  - 居中小窗，取消右侧大抽屉
- **对比**
  - 默认左右（可切换上下；手机也保留切换按钮）
  - 顶区默认收起；「详情」只放工具
  - 图例、共同片段常驻（不进详情）
  - 相似度 +「对象 n / m」进度
  - 多选：点选对象后贴基准旁并放大
  - 共同片段芯片可点跳转并高亮
  - 手机底部对象缩略条
  - 手机左右模式卡宽约 78–88vw，避免中文竖条挤成一列
- **键盘**
  - Esc 退出对比/关闭；←→ 切换对象；Enter 开始对比
- **更新**
  - 点版本号立刻出弹窗；手机加大点击区并挂到扩展根层
- **手机兼容**
  - 减弱背景模糊、隔离扩展层，减轻对酒馆顶栏颜色的影响
- **视觉**
  - 轻甜马卡龙色、大圆角、胶囊按钮；跟随酒馆主题底色


## v1.9.24

- **移动端对比顶区**：默认只留「返回 + 当前基准/对象 + 详情」；芯片/图例/共同片段收进「详情」
- **差异空壳**：不再逐行刷「对方有·基准无 / 基准有·对方无」；改为一行提示「对方另有 N 段独有内容」
- 跳过空文本单元；「只看差异」同样受益
- 窄屏强制上下全文，宽屏才显示左右切换
- 进入对比自动收起 TOC；未选中对象卡只显示摘要


## v1.9.23

- **左右 / 上下真正区分**
  - 左右：基准与对象并排；对象可横滑；长文在列内纵向滚动
  - 上下：基准与对象纵向堆叠全文，整页滚动看完
  - 修复移动端两种模式被写成同一套纵向样式、切换无效


## v1.9.22

- **根因修复：移动端对比无法下滑**
  - 对比界面曾直接挂在 `overflow:hidden` 的窗口下，且自身 `overflow:visible`，多出的全文被裁切且没有滚动容器
  - 现将对比包进 `.pmp18-content--compare` 作为唯一纵向滚动层


## v1.9.21

- **共同片段**：过滤英文字段名/停用词/无意义 token，只保留含中文或带单位数字的有效片段
- **多选未点对象**：正文纯文本、不着色、不展示共同片段
- **移动端对比重做**：对象改为纵向全文堆叠，由页面滚动看完；去掉高度锁死


## v1.9.20

- 仅两人对比时自动高亮，无需再点对象卡
- 对比默认「左右」布局，保留上下/左右切换
- **移动端滚动解锁**：去掉对比区高度锁死与 overflow 嵌套，整页可纵向滑完全文；对象区可横向滑


## v1.9.19

- **多选对比布局**：基准全文左侧固定；其余对象各一张全文卡，左右滑动浏览
- 进入多选对比后不默认选对象；点选对象卡后基准与该卡双向差异高亮
- 未选中对象卡仍显示与基准的相似片段高亮与相似度
- 移动端上下模式改为整页可滚，避免只能看到上半截
- 双击卡片/正文区仍可快速编辑


## v1.9.18

- **修复版本号错乱**：`constants.js` 未同步到 1.9.16/1.9.17，导致界面显示 1.9.15 且误报有新版；现统一为 1.9.18
- **修复移动端上下模式划不动**：workspace/focus-wrap 由 `flex:1` 改 `flex:none` 按内容撑高，整窗滚动，能看到对象全文
- **修复全文框被头像/名字框盖住**：基准条+对象条包进同一 `sticky-wrap` 一起吸顶，不再手算三层 top；hcol-head 改用 CSS 变量 `--pmp18-sticky-h` 对齐，消除重叠



- **快选栏常驻**：基准条 + 对象条都 sticky（对象条贴在基准条下），滚动时不再消失
- **DOM 顺序调整**：基准 → 对象 → 图例 → 共同片段 → 对比全文
- **对象卡缩小**：桌面 64px/头像34、移动 52px/头像28、<360px 48px/头像26
- **对象卡选中更亮**：渐变底色 + 双圈光晕 + 上浮 + 高亮描边
- **修复头像挡字**：hcol-head 背景改完全不透明主色，正文不再从头像下透出；正文块加 `scroll-margin-top` 避免被 sticky 头部盖住
- **移动端上下模式可滑动**：去掉 hcol-body 内部滚动（max-height/overflow-y），改整页自然滚动，能一路滑到底看到对方内容



- **修复移动端对比页"对象条压住图例/共同片段"**
  - 重新排列 DOM 顺序：基准条 → 对象条 → 共同片段 → 两栏全文 → 图例
  - 对象条 (objects-strip) 取消 sticky bottom，回到普通流
  - 取消 detail-meta（合并到 hcol-head）
- **hcol-head 加 sticky**：滚到共同片段或图例时，两栏（基准/对方）头部仍可见
- **修复对象条被 sticky 切走时共同片段被压**问题：共同片段现在紧跟对象条下方
- 仅基准条 sticky top: 53px


## v1.9.15

- **对比页移动端重做**：
  - 顶部工具栏 (topbar) 跟随滚动吸顶
  - 基准条 / 对象条均 sticky（基准条紧贴工具栏；对象条吸底）
  - 移动端默认上下堆叠，可切到左右并排
- **对象条改为 80px 高的横滑卡片**（sticky 在底部），双击卡片直接进入全屏编辑
- **新增目录面板 (TOC)**：右侧抽屉，按 删除/新增/替换/相同 分类，带搜索
  - 点击条目：滚动到对应段 + 1.5s 高亮闪烁
  - 搜索词在对比正文里以黄底高亮
- **短人设模式**：当文本 < 300 字符时，按句而非按行匹配
  - 共同词以粉色高亮
  - 图例注明"短人设模式"
- **连续相同段 (>3 行) 默认折叠**：
  - 仅折叠 (3+) 组的第 2..N 块
  - 点击展开/收起（折叠时显示渐变与"… 展开 N 行相同"）
- **轻动效**：TOC 抽屉滑入 / 段闪烁 / 卡片 hover 抬升
- **小屏 (< 360px)** 对象卡进一步缩窄

# Changelog


## v1.9.14

- 左上角版本号可点击：打开更新日志 / 更新弹窗（外观仍为纯文字 vX.Y.Z）
- 自动检查更新；有新版本时版本号与设置页出现 NEW，无需手动点检查
- 更新弹窗实心底框，不再显示来源 URL；打开时静默刷新不打断设置页


## v1.9.13

- **更新日志**：与 manifest 一样多源拉取，优先含最高版本号的 CHANGELOG；打开弹窗强制重新检查
- 弹窗显示实际来源 URL；按远程版本截取对应 `##` 节


## v1.9.12

- **删除后残留 [Unnamed Persona]**：删除后不再调用 `getUserAvatars`（避免 ST 按磁盘文件重新 addMissingPersonas）
- 同步逻辑不再向 `power_user` 写入「未命名」占位；并清理已无头像文件的 Unnamed 占位项


## v1.9.11

- **修复完全重复检测为空**：同名/完全重复/高度相似 的缓存互相覆盖，先算同名后完全重复被当成空结果
- 高度相似：完全相同的描述也会显示（满分）；空描述同名可检出


## v1.9.10

- **新人设检测**：缓存改为按 id/名称/描述签名自动失效，打开管理器与切换 Tab 强制刷新
- 监听 `PERSONA_CREATED` / `PERSONA_CHANGED`；标题栏增加刷新按钮
- 更新检查增加 GitHub API 源，减少 CDN 旧缓存；本地已新于远程时明确显示双方版本


## v1.9.9

- 多选删除；删除后保持管理器打开


## v1.9.8

- 语义化版本比较；删除调用 `/api/avatars/delete`


## v1.9.7

- 删除确认与头像 API 删除


## v1.9.6

- 远程仓库改为 TokkiO-O/persona-manager


## v1.9.5

- 更新检查多镜像


## v1.9.4

- jsDelivr 回退


## v1.9.3

- power-user-bridge 修复读列表


## v1.9.0

- 模块化拆分

