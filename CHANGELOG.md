# Changelog

## v1.10.1

### 修复
- **列表卡片空白 / 错位**：卡片 DOM 回到 1.9.25 结构；CSS 只保留一套横排 flex，白底深字，避免多套样式互盖
- **头像不显示**：恢复 `personaImageUrl(id)`（`/thumbnail?type=persona&file=…`），不再使用空的 `persona.avatar`
- **全屏编辑未铺满**：全屏模式不再被 `visualViewport` 限高到约 640px；`is-fullscreen` 使用 `100dvh` + 无圆角（对齐 1.9.25）

### 功能（相对 1.9.25 新增 / 加强）
- **共同片段**：按字段对齐（眼睛≠发色）；中英同义（eyes/眼睛、hair/发色等）；性格逗号特质（细心等）；短词不全文乱亮
- **多选对比**：固定基准、点选对象同屏细比、可换基准；双人进入即高亮
- **删除人设**：确认 + 备份；多选删除；删后不强制关闭扩展界面
- **备注 / 短 ID** 作小字区分同名同头
- **设置**：编辑框样式（弹窗 / 全屏）；检查更新（raw + jsDelivr）；版本号可点看日志
- **移动端**：顶区可收起、对比布局与安全区等（复杂对比仍建议电脑）

### 说明
- 对比为启发式，超长或结构差很大的文案不一定完美
- 删除只动配置里的人设记录
- 安装后请强刷（Ctrl+F5），确认左上角为 **v1.10.1**

---

## v1.9.25

见既有发布说明（马卡龙 UI、对比布局、更新弹窗等）。
